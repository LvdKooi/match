# match
Playing around with Use Case Driven Development

# running locally
To run this application locally, go to the _local_ directory in the project and run the docker-compose file. This spins up a Docker container with a Postgres database. After that just run the application.
